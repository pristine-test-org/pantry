<% content_for :title, "Edit #{@item.name}" %>

<p class="breadcrumb"><%= link_to "Items", items_path %> / <%= link_to @item.name, @item %> / Edit</p>

<header class="page-header">
  <h1>Edit <%= @item.name %></h1>
</header>

<%= render "form", item: @item %>
